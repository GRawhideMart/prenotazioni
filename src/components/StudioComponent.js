import { Typography } from '@material-ui/core';
import React from 'react';

const Studio = () => {
    return(
        <Typography variant='h1' color='secondary'>Studio component</Typography>
    )
}

export default Studio;