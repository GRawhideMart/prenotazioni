import { SCHEDULERDATA } from '../../shared/schedulerData';

export const SchedulerData = (state = SCHEDULERDATA, action) => {
    switch(action.type) {
        default:
            return state;
    }
}